import styled from 'styled-components'

export const StyledHeader = styled.header`
  grid-area: header;

  max-width: 40vw;
  font-weight: 900;
  align-items: center;
  display: flex;
  justify-content: center;
  text-align: justify;
  padding: 0 2.2rem;
`
